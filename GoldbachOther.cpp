#include <iostream>

using namespace std;

int binaryTwiceSquare(int num, int range[]) {
    return -1;
}

int main() {
    int len = 100;
    int range [len];
    for (int i; i < len; i++) range[i] = i+1;
    for (int i: range) cout << i << endl;
    return 0;
}
